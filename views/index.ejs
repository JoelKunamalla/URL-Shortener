<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
        integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css" type="text/css">
</head>

<body>
    <div class="container">
        <h1>URL Shortener</h1>
        <form action="/postUrl" method="POST" class="my-4 form-inline">
            <label for="fullUrl" class="sr-only">URL</label>
            <input required placeholder="Url to shorten" type="url" name="fullUrl" id="fullUrl" class="form-control mr-2 col">
            <button type="submit" class="btn btn-success">Submit</button>
        </form>
        <table class="table table-bordered table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Visits</th>
                </tr>
            </thead>
            <tbody>
                <% urlsArray.forEach(obj=> { %>
                    <tr>
                        <td class="text">
                            <span>
                                <a href="<%= obj.fullUrl %>">
                                    <%= obj.fullUrl %>
                                </a>
                            </span>
                        </td>
                        <td>
                            <a href="<%= obj.shortUrl %>">
                                <%= obj.shortUrl %>
                            </a>
                        </td>
                        <td>
                            <%= obj.visits %>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>

    <p class="alert alert-primary footer">Made with  <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="#e25555"
            class="bi bi-heart-fill" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M8 1.314C12.438-3.248 23.534 4.735 8 15-7.534 4.736 3.562-3.248 8 1.314z" />
        </svg>  by <a href="http://github.com/CITIZENDOT">CITIZENDOT</a>
    </p>

</body>

</html>